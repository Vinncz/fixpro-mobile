import Foundation
import Observation



/// Bridges between ``SupportiveDocumentListSwiftUIView`` and the ``SupportiveDocumentListInteractor``.
/// The former invokes and access everything listed within; and the latter supplies the implementation.
@Observable class SupportiveDocumentListSwiftUIViewModel {}
