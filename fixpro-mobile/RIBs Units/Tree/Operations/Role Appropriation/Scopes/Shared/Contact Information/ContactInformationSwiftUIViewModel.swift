import Foundation
import Observation



/// Bridges between ``ContactInformationSwiftUIView`` and the ``ContactInformationInteractor``.
/// The former invokes and access everything listed within; and the latter supplies the implementation.
@Observable class ContactInformationSwiftUIViewModel {}
